{
    "name": "query",
    "private": true,
    "type": "module",
    "workspaces": [
        "packages/*",
        "test-app"
    ],
    "scripts": {
        "dev": "bun run --filter test-app dev",
        "build": "bun run --filter test-app build",
        "check": "bun run --filter test-app check"
    },
    "devDependencies": {
        "@types/node": "^25.9.2"
    }
}